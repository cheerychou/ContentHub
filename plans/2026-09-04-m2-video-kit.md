# M2 视频语音包 + 工程清偿 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建成 M2 视频段交付物——从已生成的口播稿一键产出"配音 MP3 + 标准 SRT 字幕 + 素材清单"三件套（zip），供剪映任意版本手动导入装配；同时清偿 M0/M1 终审遗留的工程待办。

**Architecture:** edge-tts 合成配音（stream 模式拿词级时间戳，无需 ASR 对齐）→ 纯函数断句器（标点 + 字数上限双规则，落实 KNW-EXP-001 L-001"断句独立于剪切边界"）→ SRT 生成 → 素材清单 markdown → zip 单资产入 publish 区 + Derivation。复用 M1 的 monkeypatch 接缝模式：网络库（edge_tts）只在 `speech.synthesize` 一处出现，测试用合成词表。

**Tech Stack:** 在现有栈上新增 edge-tts（>=7.0,<8.0）。无新基础设施。

## Global Constraints

- 沿用全部既有约束：依赖带上限；红线（无 OpenMetadata/Neo4j/Celery/多租户/自动上传/自动剪辑/鉴权）；端口 5433/8001/8080/9000；测试库强制 contenthub_test；Conventional Commits；TDD。
- **SRT 断句是独立纯函数步骤**（L-001）：只看词序列与标点，绝不依赖任何"剪切/分块"概念；每条字幕默认上限 16 字，句末标点（。！？；!?;）优先断，逗号（，,、：:）次级断，超长硬断。
- edge_tts 只允许在 `backend/app/speech.py` 内 import（单一网络接缝，测试 monkeypatch `speech.synthesize`）。
- 视频包产物：zip 单资产，`content_type="archive"`，入 publish 区，status=publishing，自动写 Derivation（recipe_ref 留空，note 记 voice）。
- 素材清单为规则生成（不依赖 LLM）：逐句列出 + 留空建议位。
- **范围裁定**：CDD3 内容包导出与协作原语 UI **不在本计划**——协作 UI 触发条件=第二名使用者出现；CDD3 对接触发条件=CDD3 侧 MK-005 Phase 3 启动。两条件写入 PRD §5 注记（Task 5）。
- 任务完成声明绑定 commit hash（FND-GOV-001 §7）。

---

### Task 1: 合成与断句层（speech.py + subtitles.py）

**Files:**
- Modify: `backend/pyproject.toml`（dependencies 追加 `"edge-tts>=7.0,<8.0"`）
- Create: `backend/app/speech.py`
- Create: `backend/app/subtitles.py`
- Create: `backend/tests/test_speech.py`
- Create: `backend/tests/test_subtitles.py`

**Interfaces:**
- Produces:
  - `app.speech.synthesize(text: str, voice: str = "zh-CN-XiaoxiaoNeural") -> tuple[bytes, list[dict]]`：返回 (mp3 bytes, 词表)；词表元素 `{"text": str, "start": float, "end": float}`（秒）。内部 `asyncio.run(edge_tts stream)`，WordBoundary 的 offset/duration 单位 100ns → /1e7。本函数是唯一 edge_tts import 点。
  - `app.subtitles.split_sentences(words: list[dict], max_chars: int = 16) -> list[dict]`：输入词表（同上），输出句子表 `{"text": str, "start": float, "end": float}`。规则：①累积词至某词 text 以句末标点（。！？；!?;）结尾 → 断句；②累积文本长度（去标点后）≥ max_chars → 回溯最近次级标点（，,、：:）断，无次级标点则按当前词硬断；③空格跳过不计长。
  - `app.subtitles.to_srt(sentences: list[dict]) -> str`：标准 SRT（序号 / `HH:MM:SS,mmm --> HH:MM:SS,mmm` / 文本），时间格式 `%d` 补零到 3 位毫秒。
- 测试策略：test_speech 只测"模块接缝"（synthesize 被 monkeypatch 后端点可用——真正断言放 Task 2；本任务对 synthesize 本身只做 smoke：真实调用一次短句并断言 mp3 头 `\x1aE\xdf\xa3`? 不稳定——改为：真实调用标注 `@pytest.mark.network`? 项目无 marker 习惯 → **真实调用一次**（微软服务国内可达），断言返回 bytes 非空且词表非空；失败时输出跳过原因并视为环境问题上报（不静默 skip）。

- [ ] **Step 1: pyproject 加依赖并安装**

```bash
cd backend && .venv/bin/pip install "edge-tts>=7.0,<8.0" && .venv/bin/pip install -e ".[dev]"
```

pyproject dependencies 区追加（保持字母序不计）：`"edge-tts>=7.0,<8.0"`。

- [ ] **Step 2: 写失败测试 test_subtitles.py（本任务的核心测试）**

```python
from app.subtitles import split_sentences, to_srt


def _w(text, start, dur=0.2):
    return {"text": text, "start": start, "end": start + dur}


def test_split_on_sentence_punct():
    words = [_w("大家", 0.0), _w("好", 0.2), _w("。", 0.4),
             _w("今天", 0.6), _w("聊车", 0.8), _w("。", 1.0)]
    s = split_sentences(words)
    assert [x["text"] for x in s] == ["大家好。", "今天聊车。"]
    assert s[0]["start"] == 0.0 and s[1]["end"] == pytest.approx(1.2)


def test_split_on_max_chars_hard_break():
    # 无任何标点的 5 个词、每词 4 字 → 16 字上限触发硬断
    words = [_w(t, i * 0.5) for i, t in enumerate(["abcd"] * 5)]
    s = split_sentences(words, max_chars=16)
    assert len(s) == 2 and "".join(x["text"] for x in s) == "abcd" * 5


def test_split_prefers_comma_over_hard_break():
    words = [_w(t, i * 0.2) for i, t in enumerate(
        ["八个字八个字八个字四个", "，", "八个字八个字八个字四个", "。"])]
    s = split_sentences(words, max_chars=16)
    assert [x["text"] for x in s] == ["八个字八个字八个字四个，", "八个字八个字八个字四个。"]


def test_to_srt_format():
    srt = to_srt([
        {"text": "大家好。", "start": 0.0, "end": 1.2},
        {"text": "今天聊车。", "start": 1.25, "end": 2.0},
    ])
    assert srt.startswith("1\n00:00:00,000 --> 00:00:01,200\n大家好。")
    assert "2\n00:00:01,250 --> 00:00:02,000\n今天聊车。" in srt
```

（第 1 个测试需 `import pytest`。）

- [ ] **Step 3: 确认失败后实现 subtitles.py**

```python
"""SRT 字幕断句与生成（KNW-EXP-001 L-001：断句是独立步骤，只看词与标点）。"""
SENT_END = "。！？；!?;"
CLAUSE = "，,、：:"


def _plain_len(text: str) -> int:
    return len(text) - sum(text.count(c) for c in SENT_END + CLAUSE)


def split_sentences(words: list[dict], max_chars: int = 16) -> list[dict]:
    sentences: list[dict] = []
    cur: list[dict] = []

    def flush(idx: int) -> None:
        if not cur:
            return
        taken = cur[: idx + 1]
        sentences.append({
            "text": "".join(w["text"] for w in taken),
            "start": taken[0]["start"],
            "end": taken[-1]["end"],
        })
        del cur[: idx + 1]

    for w in words:
        cur.append(w)
        joined = "".join(x["text"] for x in cur)
        # 句末标点 → 断
        if joined and joined[-1] in SENT_END:
            flush(len(cur) - 1)
            continue
        # 超长 → 回溯最近次级标点，否则硬断当前词
        if _plain_len(joined) >= max_chars:
            comma_idx = next(
                (i for i in range(len(cur) - 2, -1, -1)
                 if cur[i]["text"] and cur[i]["text"][-1] in CLAUSE),
                None,
            )
            flush(len(cur) - 1 if comma_idx is None else comma_idx)
    if cur:
        flush(len(cur) - 1)
    return sentences


def _ts(sec: float) -> str:
    ms = round(sec * 1000)
    h, ms = divmod(ms, 3600000)
    m, ms = divmod(ms, 60000)
    s, ms = divmod(ms, 1000)
    return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"


def to_srt(sentences: list[dict]) -> str:
    blocks = []
    for i, x in enumerate(sentences, 1):
        blocks.append(f"{i}\n{_ts(x['start'])} --> {_ts(x['end'])}\n{x['text']}")
    return "\n\n".join(blocks) + ("\n" if sentences else "")
```

注意 `test_split_prefers_comma_over_hard_break` 的词表里标点单独成词（edge-tts 实际行为可能把标点并进前词）——实现需兼容两种情况：`cur[i]["text"][-1] in CLAUSE` 对两种形态都成立；句末同理。若真实词表把标点并入词尾，上面规则依然命中。

- [ ] **Step 4: 写 test_speech.py 并实现 speech.py**

```python
from app import speech


def test_synthesize_real_short_text():
    mp3, words = speech.synthesize("大家好。", voice="zh-CN-XiaoxiaoNeural")
    assert len(mp3) > 1000
    assert words and all(set(w) >= {"text", "start", "end"} for w in words)
    assert words[-1]["end"] > words[0]["start"]
```

```python
"""TTS 合成：edge-tts 单一网络接缝；测试经 monkeypatch speech.synthesize。"""
import asyncio

import edge_tts

DEFAULT_VOICE = "zh-CN-XiaoxiaoNeural"


async def _stream(text: str, voice: str):
    audio = bytearray()
    words: list[dict] = []
    com = edge_tts.Communicate(text, voice)
    async for chunk in com.stream():
        if chunk["type"] == "audio":
            audio.extend(chunk["data"])
        elif chunk["type"] == "WordBoundary":
            start = chunk["offset"] / 1e7
            words.append({"text": chunk["text"],
                          "start": start,
                          "end": start + chunk["duration"] / 1e7})
    return bytes(audio), words


def synthesize(text: str, voice: str = DEFAULT_VOICE) -> tuple[bytes, list[dict]]:
    return asyncio.run(_stream(text, voice))
```

- [ ] **Step 5: 全套通过**（46+~5=51 passed 量级，以实际为准；0 warnings）

- [ ] **Step 6: 提交** `git commit -m "feat: M2 语音合成与 SRT 断句层（edge-tts 词级时间戳 + 独立断句规则）"`

---

### Task 2: derive-video-kit 端点（三件套 zip）

**Files:**
- Modify: `backend/app/schemas.py`（AssetOut.content_type 等无需改；新增常量无 schema）
- Modify: `backend/app/routers/assets.py`（POST /api/assets/{id}/derive-video-kit）
- Modify: `backend/app/main.py`（无——路由已挂）
- Create: `backend/tests/test_video_kit_api.py`

**Interfaces:**
- Consumes: `speech.synthesize`（monkeypatch 接缝）、`subtitles.split_sentences/to_srt`、`get_storage`、Derivation 模式
- Produces:
  - `POST /api/assets/{speech_id}/derive-video-kit`：multipart `voice`（默认 zh-CN-XiaoxiaoNeural）、可选 `title`（默认 "{原题} 语音包"）→ 201 AssetDetail。守卫：资产须有 text_content（422）。流程：synthesize → split_sentences → to_srt → 素材清单 markdown（逐句 `[NN] 句子 ｜ 建议画面：＿＿＿`）→ zip（`audio.mp3` + `subtitle.srt` + `shotlist.md`）→ publish 区 zip 资产（content_type="archive"，meta={"kind":"video_kit","voice","sentences":N}）+ Derivation
  - 失败映射：edge-tts 网络类异常（`Exception` 兜底但区分 `httpx`/`asyncio.TimeoutError`？edge-tts 用 websockets——统一 `except Exception → 502 "TTS 服务不可达: ..."`；LLMNotConfigured 不涉及）
  - voice 白名单常量 `app.speech.VOICES = {"晓晓（女）": "zh-CN-XiaoxiaoNeural", "云健（男）": "zh-CN-YunjianNeural", "云希（男）": "zh-CN-YunxiNeural"}`（键为中文展示名，值传 edge-tts；端点收展示名，未知 422）
- 测试：FakeSynth（monkeypatch `app.routers.assets` 引用的 speech.synthesize——注意端点须 `from .. import speech` 模块限定调用，与 rendering 同模式）断言 zip 结构（zipfile.ZipFile 读三文件名与内容片段）、Derivation、meta；真实 TTS 链路留给 Task 4 验收。

- [ ] **Step 1: 写失败测试**

```python
import io
import zipfile


def test_video_kit_end_to_end(client, db_session, monkeypatch):
    master = client.post(
        "/api/assets", data={"zone": "master", "title": "定稿文章"},
        files={"file": ("a.md", "第一句。第二句比较长一些一些。".encode(),
                        "text/markdown")}).json()

    captured = {}

    def fake_synth(text, voice="zh-CN-XiaoxiaoNeural"):
        captured.update(text=text, voice=voice)
        words = [{"text": c, "start": i * 0.2, "end": i * 0.2 + 0.18}
                 for i, c in enumerate(text.replace("。", "。"))]
        return b"FAKE_MP3", words

    from app import speech as speech_mod
    monkeypatch.setattr(speech_mod, "synthesize", fake_synth)

    resp = client.post(
        f"/api/assets/{master['id']}/derive-video-kit",
        data={"voice": "晓晓（女）", "title": "跨品牌售后语音包"})
    assert resp.status_code == 201
    body = resp.json()
    assert body["zone"] == "publish"
    assert body["content_type"] == "archive"
    assert body["meta"]["kind"] == "video_kit"
    assert body["meta"]["voice"] == "晓晓（女）"
    assert body["upstream"][0]["source_asset_id"] == master["id"]
    assert captured["voice"] == "zh-CN-XiaoxiaoNeural"
    assert captured["text"].startswith("第一句。")

    detail = client.get(f"/api/assets/{body['id']}").json()
    png_bytes = __import__("urllib.request").urlopen(detail["file_url"]).read()
    zf = zipfile.ZipFile(io.BytesIO(png_bytes))
    assert set(zf.namelist()) == {"audio.mp3", "subtitle.srt", "shotlist.md"}
    srt = zf.read("subtitle.srt").decode("utf-8")
    assert "1\n" in srt and "第一句。" in srt


def test_video_kit_requires_text(client, db_session):
    img = client.post(
        "/api/assets", data={"zone": "master", "title": "图"},
        files={"file": ("a.png", b"\x89PNG", "image/png")}).json()
    resp = client.post(f"/api/assets/{img['id']}/derive-video-kit", data={})
    assert resp.status_code == 422
```

（真实下载 file_url 依赖 FakeStorage presigned `fake://` 前缀 → **urlopen 不可用**。修正：直接从 `app.storage` 拿不到——改为通过 detail.file_url 前缀判断 + 用 client 内部 FakeStorage 实例断言。**实现时改为**：fixture 返回 fake 实例，测试 `fake.get(zone, key)` 拿 zip bytes。）

- [ ] **Step 2: 确认失败后实现**（模式复用 Task M1-5/6：模块限定 `speech.synthesize`；zip 用 `zipfile.ZipFile(io.BytesIO(), "w", zipfile.ZIP_DEFLATED)`；失败映射 502；voice 展示名 → 值映射，未知 422 列出可选）

- [ ] **Step 3: 全套通过**（51+2=53 量级）
- [ ] **Step 4: 提交** `git commit -m "feat: M2 视频语音包端点（配音+SRT+素材清单 zip 派生）"`

---

### Task 3: 平台常量 API 化 + 前端视频包表单

**Files:**
- Modify: `backend/app/routers/assets.py` 或新建 `backend/app/routers/meta.py`（GET /api/meta/platforms）
- Modify: `backend/app/main.py`（若新建路由文件则挂载）
- Modify: `frontend/src/App.tsx`（视频包表单 + 平台列表改运行时获取）
- Modify: `frontend/src/api.ts`、`frontend/src/types.ts`
- Create: `backend/tests/test_meta_api.py`

**Interfaces:**
- Produces:
  - `GET /api/meta/platforms` → `{"cover": ["微信公众号·横版", ...6 项], "entry_urls": {基础平台名: url}, "voices": {"晓晓（女）": "zh-CN-XiaoxiaoNeural", ...}}`——前端删掉本地 PLATFORMS/PUBLISH_ENTRY_URLS 硬编码（types.ts 保留类型），App 启动时 fetch 一次
  - 详情面板：publish 区 `content_type === "markdown"` 资产 → "生成视频语音包"表单（音色下拉来自 voices 键 + 标题可选）→ 成功后刷新详情（下游出现 zip 资产）
  - zip 资产（archive）显示下载链接（file_url 现有逻辑已覆盖）

- [ ] **Step 1: 失败测试**（meta 端点三键齐全、voices 与 cover 键数；前端无测试 runner——build+冒烟）
- [ ] **Step 2: 实现**（meta 端点直接从 cover_specs/speech 引常量，单一来源落地——终审遗留"平台常量双源"就此消除）
- [ ] **Step 3: `npm run build` + 全栈冒烟**（`docker compose up -d --build api frontend`；:8080 拉一次 /api/meta/platforms；对一篇真实口播稿走通表单→zip 下载）
- [ ] **Step 4: 提交** `git commit -m "feat: M2 平台常量 API 化 + 前端视频语音包表单"`

---

### Task 4: 工程清偿批（终审遗留）

**Files:**
- Modify: `backend/app/routers/assets.py`（derive-text 网络级异常→502；render-cover chromium 失败→503）
- Modify: `backend/alembic/versions/*`（新迁移：Derivation.created_at → timestamptz）
- Create: `backend/scripts/cleanup_orphans.py`（对比 MinIO 对象 key 与 assets.object_key，列出/删除孤儿；dry-run 默认）
- Modify: `Makefile`（`preflight` 目标：docstd check + pytest + alembic upgrade head）
- Create: `backend/tests/test_cleanup_orphans.py`（FakeStorage 造孤儿 → dry-run 列出 → 执行删除）

**Interfaces:**
- Produces:
  - derive-text：`httpx.ConnectError/TimeoutException` → 502（现仅 HTTPStatusError）
  - render-cover：Playwright 启动/渲染异常 → 503 "渲染引擎不可用"
  - Derivation.created_at 为 timestamptz（新迁移，upgrade/downgrade 对称）
  - `make preflight` 一键本地门禁（替代 CI 红线下的质量闸）
  - `make cleanup-orphans DRY=1|0`：默认 dry-run

- [ ] **Step 1: 每项先测试后实现**（异常映射用 monkeypatch 抛异常断言状态码；迁移验证 upgrade→downgrade→upgrade；cleanup 用 FakeStorage 双实现——真实 MinIO 模式同 M0 storage 注入）
- [ ] **Step 2: 全套通过 + `make preflight` 全绿**
- [ ] **Step 3: 提交** `git commit -m "fix: M2 工程清偿（502/503 映射、Derivation 时区、孤儿清理、preflight 门禁）"`

---

### Task 5: M2 验收 + PRD/文档同步

- [ ] **真实链路验收**（经 :8080）：挑真实定稿 → derive-text 口播稿 → derive-video-kit（真实 edge-tts）→ 下载 zip → 解压验证 mp3 可播（`afplay` 或 sips 级校验字节数）、SRT 断句人工评定（按 L-001 标准：无字墙、断在标点）、shotlist 逐句对应 → **SRT 导入剪映 11.4 实测一次**（剪映支持字幕导入 SRT；此步由用户执行，结果回填）
- [ ] **PRD 更新**：§5 M2 行改写为实际范围（视频语音包 + 工程清偿），CDD3 对接/协作原语标注触发条件顺延；M2 行验收标记按 §7 绑定 commit；version → 2.2，变更记录补行
- [ ] **提交** `git commit -m "docs: M2 验收通过并绑定证据 (PRD-MVP-001)"`

---

### Task 6: 终审 + 合入 main

- 全分支 review-package 终审（含 ledger triage）；Critical/Important 修复；全测试；合入 main；删分支；推送；持久记忆更新（M2 完成态）

## Self-Review 记录

- **Spec 覆盖**：B 方案三件套（T1 断句/SRT、T2 端点、T3 前端、T5 验收含剪映导入实测）；终审遗留（T4：502/503/tz/orphan/preflight；平台常量双源在 T3 消除）；范围裁定（CDD3/协作 UI 顺延，Task 5 落 PRD）。✔
- **占位符扫描**：无 TBD；voice 白名单为具体值；edge-tts 词表形态（标点是否并入词尾）标注了兼容两种形态的实现要求。✔
- **类型一致性**：`speech.synthesize` 签名在 T1 定义、T2 消费；meta 端点键（cover/entry_urls/voices）与前端消费对应。✔
- **风险标注**：edge-tts 词级时间戳的标点形态与 SRT 端到端精度需真实验收确认（T1 单测只锁断句规则，T5 锁全链路）；剪映导入 SRT 由用户实测回填。
